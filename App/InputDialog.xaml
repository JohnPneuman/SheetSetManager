<Window x:Class="SheetSetEditor.InputDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="" Height="155" Width="340"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False">
    <Grid Margin="14,12,14,12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="10"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="14"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock x:Name="PromptLabel" Grid.Row="0" Text="Invoer:" TextWrapping="Wrap"/>
        <TextBox   x:Name="InputBox"    Grid.Row="2" Height="24" Padding="3,0" VerticalContentAlignment="Center"/>
        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="OK"         Width="65" Height="26" Margin="0,0,6,0"
                    IsDefault="True" Click="OkButton_Click"/>
            <Button Content="Annuleren"  Width="80" Height="26" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>
